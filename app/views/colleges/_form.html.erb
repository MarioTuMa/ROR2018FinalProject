<%= form_with(model: college, local: true) do |form| %>
  <% if college.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(college.errors.count, "error") %> prohibited this college from being saved:</h2>

      <ul>
      <% college.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :college_name %>
  </div>

  <div class="field">
    <%= form.label :private %>
    <%= form.check_box :private, id: :college_private %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
